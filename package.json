{
  "name": "my-project",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.3.0",
  "scripts": {
    "dev:backend": "dotenv -- pnpm --filter ./backend dev",
    "dev": "concurrently \"dotenv -- pnpm --filter frontend dev\" \"dotenv -- pnpm --filter ./backend dev\"",
    "build:backend": "pnpm --filter ./backend build",
    "build": "pnpm --filter frontend build && pnpm --filter backend build && pnpm --filter shared build ",
    "start:backend": "pnpm --filter backend start"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2"
  }
}
