{
  "name": "my-project",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.3.0",
  "scripts": {
    "dev:backend": "dotenv -- pnpm --filter ./backend start:dev",
    "dev": "dotenv -- pnpm --parallel --stream -r run start:dev",
    "build:backend": "pnpm --filter ./backend build",
    "build": "pnpm --stream -r run build",
    "start": "dotenv -- pnpm --stream -r run start",
    "start:backend": "dotenv -- pnpm --filter backend start"
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2"
  }
}
