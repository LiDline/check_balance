{
  "name": "shared",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start:dev": "tsc -w --incremental"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20"
  }
}
